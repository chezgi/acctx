# Corporate-tax adjustment semantics

- `direction`: `add` or `deduct`.
- Only `review_status` values `approved` or `accepted` are included.
- `book_profit_irr` and `tax_credits_irr` are CLI flags.
- Benefits, exemptions, and credits require evidence and human review.
